import { ActivitiesType, Durations } from './data';

export default function ActivityCard({
  duration,
  activity,
}: {
  duration: Durations;
  activity: ActivitiesType;
}) {
  return <div>ActivityCard</div>;
}
